{
  "mcpServers": {
    "rider-search": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/proxy/src/server.js"],
      "env": {
        "RIDER_MCP_SSE_URL": "${RIDER_MCP_SSE_URL}",
        "RIDER_MAX_RESULTS": "50"
      }
    }
  }
}
