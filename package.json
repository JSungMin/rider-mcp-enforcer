{
  "name": "rider-mcp-enforcer-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling (lint/format/test) for the rider-mcp-enforcer marketplace. Not published; the plugins live in proxy/ and gamedev-log-analyzer/.",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:proxy": "node --test proxy/test",
    "eval": "node gamedev-log-analyzer/eval/run.mjs",
    "test": "npm run test:proxy && npm run eval",
    "bump": "node scripts/bump.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^10.8.1",
    "globals": "^17.7.0",
    "prettier": "^3.9.5"
  },
  "engines": {
    "node": ">=18"
  }
}
